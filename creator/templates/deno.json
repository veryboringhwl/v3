{
  "imports": {
    "/hooks/": "<INSERT_HOOKS_PATH>",
    "/modules/": "./modules/"
  },
  "tasks": {
    "build": "creator build -i modules -o modules -c classmap.json",
    "watch": "creator build -i modules -o modules -c classmap.json -w --dev",
    "release": "creator release",
    "lint": "deno run -A npm:@biomejs/biome check --write .",
    "format": "deno run -A npm:@biomejs/biome format --write ."
  },
  "compilerOptions": {
    "jsx": "react-jsx",
    "noImplicitOverride": true,
    "lib": ["deno.ns", "DOM", "DOM.Iterable", "ESNext"]
  },
  "lint": { "exclude": ["**/*"] },
  "format": { "exclude": ["**/*"] }
}
